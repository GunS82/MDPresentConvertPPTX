# MDPresentConvertPPTX

Simple converter from Markdown to PowerPoint slides using `python-pptx`.

## Usage

```bash
pip install -r requirements.txt
python -m md2pptx.cli samples/slides.md -o slides.pptx
```
